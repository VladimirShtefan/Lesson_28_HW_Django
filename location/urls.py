from django.urls import path

from location import views


urlpatterns = [
   # path('', views.LocationListView.as_view(), name='ad_get'),
   # path('<int:pk>/', views.AdDetailView.as_view(), name='detail_ads'),
]
